// Define the companyName variable as 'Scuber' and declare it as a constant
const companyName = 'Scuber';

// Define the mostProfitableNeighborhood variable as 'Chelsea' and declare it using let
let mostProfitableNeighborhood = 'Chelsea';

// Define the companyCeo variable as 'Susan Smith' and declare it using let
let companyCeo = 'Susan Smith';

// Export the variables
module.exports = {
  companyName,
  mostProfitableNeighborhood,
  companyCeo
};